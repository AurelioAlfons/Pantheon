"""Integration adapters -- real and mock versions of anything that talks to the outside world.

The rule from CLAUDE.md: agents never know which they're using. MODE is resolved once,
in factory.py, and nowhere else.
"""
